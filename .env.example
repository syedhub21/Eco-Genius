# Database URL — local SQLite (optional, app degrades gracefully without it)
DATABASE_URL="file:./db/custom.db"

# For production (Netlify/Vercel), use PostgreSQL instead:
# DATABASE_URL="postgresql://user:password@host:port/database"
